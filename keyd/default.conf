# Applies to every keyboard not already claimed by a device-specific config
# (the Apple Magic Keyboard has its own file: apple-magic-keyboard.conf).
[ids]
*
-05ac:029c

[main]
# Disable Caps Lock; emit F13 so i3 can bind it (e.g. fullscreen toggle).
capslock = f13
