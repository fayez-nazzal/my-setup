# tmuxscope scopes
# One line per scope: name = path [path ...]
# Paths are intentionally expressed relative to $HOME for portability.
repos = ~/repos
ruby = ~/rayyan/rayyan-ruby
feos = ~/rayyan/rayyan-feos
personalization = ~/rayyan/rayyan-personalization
