# tmuxscope scopes
# One line per scope: name = path [path ...]
# Paths are intentionally expressed relative to $HOME for portability.
arch = ~/app/app-frontend*
ruby = ~/app/app-ruby*
app = ~/app
repos = ~/repos
tools = ~/repos/tools
claude = ~/.claude
agentic-monorepo = ~/repos/agentic-monorepo*
